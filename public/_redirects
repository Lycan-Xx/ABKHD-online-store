# Cloudflare Pages Redirects

# All API routes handled by Astro's Cloudflare adapter - no redirects needed
# Keeping file for potential future redirects

# SPA fallback - uncomment if using client-side routing
# /_next/static/:path /_next/static/:path 200
# /admin/* /index.html 200